<?xml version="1.0" encoding="utf-8" standalone="yes"?>
{{- /*
  Same shape as Hugo's built-in sitemap template, except the home page's
  lastmod is pinned to the current build time. Hugo otherwise derives it
  from the newest dated content page, which understates how often the
  homepage actually changes (next-meeting card, schedule data) via the
  daily scheduled rebuild in .github/workflows/pages.yml.
*/ -}}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{ range .Data.Pages -}}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if .IsHome }}
    <lastmod>{{ safeHTML ( now.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ else if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
  {{ end -}}
</urlset>
